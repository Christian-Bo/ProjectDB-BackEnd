# =========================
# App
# =========================
spring.application.name=nexttech-backend
server.port=${PORT:8080}

# =========================
# DataSource (SQL Server)
# =========================
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.url=jdbc:sqlserver://SistemaNextTechStore_v2.mssql.somee.com;databaseName=SistemaNextTechStore_v2;encrypt=true;TrustServerCertificate=true;
spring.datasource.username=NexTechSolution_SQLLogin_1
spring.datasource.password=ab75t58oq5

# =========================
# JPA / Hibernate
# =========================
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# =========================
# Jackson
# =========================
spring.jackson.serialization.write-dates-as-timestamps=false

# =========================
# Logging
# =========================
logging.level.root=INFO
logging.level.com.nexttechstore.nexttech_backend=DEBUG

# =========================
# SQL init (optional)
# =========================
spring.sql.init.mode=never

# =========================
# Swagger / Springdoc
# =========================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger
springdoc.packages-to-scan=com.nexttechstore.nexttech_backend.controller
springdoc.paths-to-match=/api/**

# =========================
# CORS (Global) - read by your CorsConfig (WebMvcConfigurer)
# =========================
# OrÃ­genes permitidos (patrones). Incluye tu dominio exacto de Koyeb:
app.cors.allowed-origins=https://respective-sheena-chris-project-74413f7f.koyeb.app,http://localhost:*,http://127.0.0.1:*,https://*.koyeb.app
app.cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS,HEAD
app.cors.allowed-headers=Content-Type,Authorization,Accept,Origin,X-Requested-With,X-User-Id
app.cors.exposed-headers=Location,Link
app.cors.allow-credentials=false
app.cors.max-age=3600
app.cors.path-pattern=/api/**

logging.level.org.springdoc=DEBUG
logging.level.org.springframework.web=DEBUG
